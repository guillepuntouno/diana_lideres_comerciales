{
  "version": "0.2.0",
  "configurations": [
    {
      // Configuración original para lanzar Chrome
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome against localhost",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}"
    },
    {
      // NUEVA configuración para depurar Flutter Web manualmente
      "name": "Flutter Web on port 51052 (manual launch)",
      "request": "launch",
      "type": "chrome",
      "url": "http://localhost:51052",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
